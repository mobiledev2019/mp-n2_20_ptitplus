<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.btl">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ThoiKhoaBieuActivity"></activity>
        <activity android:name=".TrangChuActivity" />
        <activity android:name=".DiemActivity" />
        <activity android:name=".ThoiKhoaBieuGridActivity" />
        <activity android:name=".ThongBaoActivity" />
        <activity android:name=".LoiNhacCalendarActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".LoiNhacActivity" />
        <activity android:name=".TinTucActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>